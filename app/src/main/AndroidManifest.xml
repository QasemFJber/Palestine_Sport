<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".controllers.AppController"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PalestineSport"
        tools:targetApi="31">
        <activity
            android:name=".screens.PlaygroundReservationActivity"
            android:exported="false" />
        <activity
            android:name=".MatchDataActivity"
            android:exported="false" />
        <activity
            android:name=".MatchScheduleActivity"
            android:exported="false" />
        <activity
            android:name=".screens.DatasActivity"
            android:exported="false" />
        <activity
            android:name=".screens.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>